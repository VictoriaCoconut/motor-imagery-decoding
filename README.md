# motor-imagery-decoding
Motor imagery EEG decoding with classical ML — a real BCI project built from the ground up. CSP+LDA and Riemannian pipelines, evaluated per-subject with MOABB. Doubles as a documented learning path: each step is a self-contained class, from "what is learning?" to a multi-pipeline benchmark.
